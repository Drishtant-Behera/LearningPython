# we need to import this for arraysimport numpy as np# Mutablelist_1 = ['History', 'Math', 'Physics', 'CompSci']list_2 = list_1print(list_1)print(list_2)list_1[0] = 'Art'tuple_1 = ['History', 'Math', 'Physics', 'CompSci']tuple_2 = tuple_1tuple_1[0] = 'Art'print(tuple_1)print(tuple_2)# Setscs_courses = {'History', 'Math', 'Physics', 'CompSci'}print(cs_courses)# Empty Listsempty_list = []empty_list = list()# Empty Tuplesempty_tuple = ()empty_tuple = tuple()# Empty Setsempty_set = {}empty_set = set()# Arraya = np.ones(10)type(a[0])  # the zeros in the array are float valuesprint(a)a = np.linspace(2, 10, 5)  # this says the computer to count by 2s all the way till ten and have 5 integers initprint(a)